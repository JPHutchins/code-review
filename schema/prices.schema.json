{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/JPHutchins/code-review/main/schema/prices.schema.json",
  "title": "Code Review \u2014 price map",
  "description": "A date-stamped map of per-model token prices (USD per 1M tokens) the commenter uses to recompute canonical cost from the abstract result envelope's `models` array (SPEC \u00a76.1). The CLI's `vendor_cost_usd` is vendor-priced and wrong for non-Anthropic backends; this map is the recomputation source. Kept free of $ref/$defs to match the findings schema's house style (inlining is not strictly required here \u2014 the price map is never passed to a model \u2014 but consistency with findings.schema.json is preferred).",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "_updated",
    "_unit",
    "models"
  ],
  "properties": {
    "_comment": {
      "type": "string",
      "description": "Human note; not parsed. Should remind that prices drift and point at the cost formula (SPEC \u00a76.2)."
    },
    "_updated": {
      "type": "string",
      "description": "ISO date (YYYY-MM-DD) the prices were last verified against the provider's pricing page. Prices drift; keep this current."
    },
    "_unit": {
      "type": "string",
      "description": "Unit the prices are expressed in per 1M tokens, e.g. \"USD per 1M tokens\"."
    },
    "models": {
      "type": "object",
      "description": "Map of model identifier \u2192 per-token prices. Keys are the model identifiers as they appear in the result envelope's `models[].model`. A model's value is EITHER a flat all-day price (unchanged) OR a set of UTC time-of-day slots (issue #170), for a provider with peak/off-peak rates.",
      "additionalProperties": {
        "oneOf": [
          {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "in",
              "out",
              "cache_read",
              "cache_write"
            ],
            "properties": {
              "in": {
                "type": "number",
                "minimum": 0,
                "description": "Price per 1M input tokens (cache-miss)."
              },
              "out": {
                "type": "number",
                "minimum": 0,
                "description": "Price per 1M output tokens."
              },
              "cache_read": {
                "type": "number",
                "minimum": 0,
                "description": "Price per 1M cache-read tokens (prompt-cache hits)."
              },
              "cache_write": {
                "type": "number",
                "minimum": 0,
                "description": "Price per 1M cache-write tokens (prompt-cache population)."
              }
            }
          },
          {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "slots"
            ],
            "description": "UTC time-of-day pricing (issue #170). `slots` MUST partition the full 24h UTC day with no gap and no overlap: each is a half-open [utc_from, utc_to) window, and a slot whose utc_to <= utc_from wraps past midnight. `weekend_slots` is an optional second partition used on Saturdays and Sundays in BEIJING time (issue #216) \u2014 the Beijing weekend runs Friday 16:00 UTC to Sunday 16:00 UTC, so weekend-ness is NOT a property of the UTC date; it must partition the day exactly as `slots` does. A model with no `weekend_slots` uses `slots` every day. Cost recomputation selects the map first, then the one slot covering the run's UTC instant; a run's time-of-day matching zero or more than one slot is a misconfiguration that warns loudly and prices that model at $0 (never silently wrong).",
            "properties": {
              "slots": {
                "type": "array",
                "minItems": 1,
                "items": {
                  "type": "object",
                  "additionalProperties": false,
                  "required": [
                    "utc_from",
                    "utc_to",
                    "in",
                    "out",
                    "cache_read",
                    "cache_write"
                  ],
                  "properties": {
                    "utc_from": {
                      "type": "string",
                      "pattern": "^([01][0-9]|2[0-3]):[0-5][0-9]$",
                      "description": "Slot start, HH:MM in UTC (inclusive)."
                    },
                    "utc_to": {
                      "type": "string",
                      "pattern": "^([01][0-9]|2[0-3]):[0-5][0-9]$",
                      "description": "Slot end, HH:MM in UTC (exclusive); a value <= utc_from wraps past midnight."
                    },
                    "in": {
                      "type": "number",
                      "minimum": 0,
                      "description": "Price per 1M input tokens (cache-miss) in this slot."
                    },
                    "out": {
                      "type": "number",
                      "minimum": 0,
                      "description": "Price per 1M output tokens in this slot."
                    },
                    "cache_read": {
                      "type": "number",
                      "minimum": 0,
                      "description": "Price per 1M cache-read tokens (prompt-cache hits) in this slot."
                    },
                    "cache_write": {
                      "type": "number",
                      "minimum": 0,
                      "description": "Price per 1M cache-write tokens in this slot."
                    }
                  }
                }
              },
              "weekend_slots": {
                "type": "array",
                "minItems": 1,
                "items": {
                  "type": "object",
                  "additionalProperties": false,
                  "required": [
                    "utc_from",
                    "utc_to",
                    "in",
                    "out",
                    "cache_read",
                    "cache_write"
                  ],
                  "properties": {
                    "utc_from": {
                      "type": "string",
                      "pattern": "^([01][0-9]|2[0-3]):[0-5][0-9]$",
                      "description": "Slot start, HH:MM in UTC (inclusive)."
                    },
                    "utc_to": {
                      "type": "string",
                      "pattern": "^([01][0-9]|2[0-3]):[0-5][0-9]$",
                      "description": "Slot end, HH:MM in UTC (exclusive); a value <= utc_from wraps past midnight."
                    },
                    "in": {
                      "type": "number",
                      "minimum": 0,
                      "description": "Price per 1M input tokens (cache-miss) in this slot."
                    },
                    "out": {
                      "type": "number",
                      "minimum": 0,
                      "description": "Price per 1M output tokens in this slot."
                    },
                    "cache_read": {
                      "type": "number",
                      "minimum": 0,
                      "description": "Price per 1M cache-read tokens (prompt-cache hits) in this slot."
                    },
                    "cache_write": {
                      "type": "number",
                      "minimum": 0,
                      "description": "Price per 1M cache-write tokens in this slot."
                    }
                  }
                },
                "description": "Optional: the slot partition used on Beijing Saturdays and Sundays. Same shape and same 24h-partition requirement as `slots`."
              }
            }
          }
        ]
      }
    }
  }
}
